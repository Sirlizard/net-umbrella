# Copy this file to .env or .env.local and fill in your public browser key
# This key will be exposed in the browser. Restrict it by HTTP referrer (your domain) and by API (Maps JavaScript API + Places API) in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=YOUR_PUBLIC_BROWSER_KEY_HERE
